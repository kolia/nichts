#!/usr/bin/env ion

cp /etc/nixos/configuration.nix configuration.nix

for file in [ redshift.conf ]
   cp {~/,home/}.config/$file
end

for folder in [ i3 i3status ]
   rsync -av {~/,home/}.config/$folder/
end

